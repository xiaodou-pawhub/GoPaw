# GoPaw 环境变量配置示例
# 复制此文件为 .env，填入实际值，docker compose 会自动读取。
# .env 不应提交到 Git（已在 .gitignore 中忽略）。

# Web UI 访问密码
# 留空则每次重启时自动生成随机 token 并打印到日志
# 生产环境建议设置一个固定值，否则重启后需要重新查日志获取 token
GOPAW_ADMIN_TOKEN=your-admin-token-here
