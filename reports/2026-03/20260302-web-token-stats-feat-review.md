# Code Review 报告（标准）

**审核日期**: 2026-03-02
**审核者**: Codex (GPT-5)
**被审核报告**: `reports/2026-03/20260302-web-token-stats-feat.md`
**变更类型**: feat
**审核范围**: 会话 Token 统计 API 与前端展示（`GetSessionStats`、`api/agent.ts`、`Chat.vue`）

---

## 📊 评分汇总

| 维度 | 得分 | 满分 |
|------|------|------|
| 一：代码质量与规范 | 14 | 25 |
| 二：业务逻辑与架构 | 10 | 25 |
| 三：安全性与数据隐私 | 8 | 15 |
| 四：可测试性 | 5 | 10 |
| 五：工程规范与兼容性 | 6 | 10 |
| 专项（feat/fix/refactor/frontend）| 6 | 15 |
| **总分** | **49** | **100** |

**合并决策**：❌ 不得合并

---

## 🔍 各维度详细评价

### 维度一：代码质量与规范（14/25）

- **1.1 命名规范（3/5）**：
  命名基本清晰，但 `getSessionStats` 返回 `Promise<any>`，与 `SessionStats` 类型定义不一致。`web/src/api/agent.ts:40`

- **1.2 可读性与复杂度（3/5）**：
  UI 展示清晰。  
  扣分点：统计加载与会话加载、SSE 状态交织在 `Chat.vue`，复杂度偏高。`web/src/pages/Chat.vue:236-244,272-285,369-374`

- **1.3 代码复用性 DRY（3/5）**：
  格式化函数有复用价值。  
  扣分点：`formatTokens` 分支重复，`>=10000` 与 `>=1000` 返回逻辑相同。`web/src/pages/Chat.vue:250-251`

- **1.4 错误处理与健壮性（3/5）**：
  统计加载失败仅 `console.error`，缺少用户反馈或降级提示。`web/src/pages/Chat.vue:241-243`

- **1.5 魔法值与硬编码（2/5）**：
  后端统计返回固定常量，属于硬编码占位实现。`internal/server/handlers/agent.go:152-158`

---

### 维度二：业务逻辑与架构（10/25）

- **2.1 逻辑正确性（3/10）**：
  当前 `GetSessionStats` 为固定 mock，不随会话变化，无法满足“实时展示当前会话 token 消耗”的核心业务语义。`internal/server/handlers/agent.go:148-159`

- **2.2 架构符合度（4/8）**：
  分层位置正确（handler + API + 页面）。  
  扣分点：mock 接口没有显式 mock 标志或版本隔离，容易被误认为真实数据。

- **2.3 性能与资源管理（3/7）**：
  每次切会话、SSE 结束都触发统计刷新是合理的。  
  但在当前静态 mock 情况下，这些调用全是无效请求，增加了不必要开销。

---

### 维度三：安全性与数据隐私（8/15）

- **3.1 输入校验（3/5）**：
  `session_id` 从 path 获取但未校验存在性/格式。`internal/server/handlers/agent.go:150`

- **3.2 敏感信息处理（3/5）**：
  统计接口不含敏感字段，风险较低。  
  扣分点：错误日志仍可能包含上下文细节，建议统一错误码。

- **3.3 权限控制（2/5）**：
  新增 `/api/agent/sessions/:id/stats` 未见鉴权隔离逻辑，多用户场景有越权读取风险。

---

### 维度四：可测试性（5/10）

- **4.1 职责单一性（2/5）**：
  `GetSessionStats` 当前仅返回固定结构，测试价值有限。  
  前端 `Chat.vue` 统计与聊天逻辑耦合。

- **4.2 依赖可注入性（3/5）**：
  后端 handler 依赖注入结构合理；但真实统计逻辑尚未抽象接口，后续可测性未知。

---

### 维度五：工程规范与兼容性（6/10）

- **5.1 代码风格一致性（2/4）**：
  前端已有 `SessionStats` 类型，但 API 使用 `any`，风格不一致。`web/src/types/index.ts:72-78`、`web/src/api/agent.ts:40`

- **5.2 新引入依赖评估（3/3）**：
  未新增依赖。

- **5.3 接口与 API 兼容性（1/3）**：
  新增接口是增量兼容。  
  但 mock 数据固定，语义上不兼容“真实统计”预期，需明确标识为临时实现。

---

### 类型专项（6/15）

#### feat 专项：验收标准覆盖度
- **验收标准逐项确认（3/8）**：
  UI 徽章和刷新时机实现了；但核心“实时 token 消耗”并未实现，仅固定数字 mock。

- **接口设计合理性（3/7）**：
  `GET /sessions/:id/stats` 路径合理。  
  建议返回字段增加 `is_mock: true`（临时阶段）避免误导。

---

## 🚀 优化建议（按优先级）

### P0 — 必须修复（不得合并）

1. **[问题]**：统计接口返回固定 mock，功能与报告目标“实时统计”不一致。  
   **[定位]**：`internal/server/handlers/agent.go:152-158`  
   **[建议]**：接入真实 memory/token 统计（按 session 聚合消息 token），或在 UI 明确标注 “Mock 数据” 并降级为实验功能。

### P1 — 强烈建议（整改后合并）

1. **[问题]**：`getSessionStats` 类型不安全，未使用 `SessionStats`。  
   **[定位]**：`web/src/api/agent.ts:40`  
   **[建议]**：改为 `Promise<SessionStats>` 并补充接口返回字段校验。

2. **[问题]**：`formatTokens` 条件重复，维护性差。  
   **[定位]**：`web/src/pages/Chat.vue:250-251`  
   **[建议]**：合并分支，或按 `k/m` 阶梯做统一格式化函数。

3. **[问题]**：统计接口缺少会话参数校验。  
   **[定位]**：`internal/server/handlers/agent.go:150`  
   **[建议]**：增加空值校验并返回 400。

### P2 — 锦上添花（可遗留，记录到 tech-debt）

1. **[问题]**：统计加载失败仅日志输出，不提示用户。  
   **[定位]**：`web/src/pages/Chat.vue:241-243`  
   **[建议]**：增加轻量 UI 提示（例如徽章降级为 `--` 并给 tooltip 错误信息）。

---

## 💡 代码亮点

- 统计展示放在聊天头部并配合 tooltip 细分用户/助手 token，信息架构清晰，交互成本低。`web/src/pages/Chat.vue:56-71`
